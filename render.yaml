services:
  - type: web
    name: flask-ocr-api
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app -b 0.0.0.0:10000
    envVars:
      - key: PORT
        value: 10000
    plan: free
    autoDeploy: true
